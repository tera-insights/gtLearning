## These are the tables for each type of rule, which includes the heady and body
## predicates, the classes of the arguments, and the weight.
library(gtBase)

Create(Rules_1,
       Head   = Int,
       Body1  = Int,
       Class1 = Int,
       Class2 = Int,
       Weight = Double)

Create(Rules_2,
       Head   = Int,
       Body1  = Int,
       Class1 = Int,
       Class2 = Int,
       Weight = Double)

Create(Rules_3,
       Head   = Int,
       Body1  = Int,
       Body2  = Int,
       Class1 = Int,
       Class2 = Int,
       Class3 = Int,
       Weight = Double)

Create(Rules_4,
       Head   = Int,
       Body1  = Int,
       Body2  = Int,
       Class1 = Int,
       Class2 = Int,
       Class3 = Int,
       Weight = Double)

Create(Rules_5,
       Head   = Int,
       Body1  = Int,
       Body2  = Int,
       Class1 = Int,
       Class2 = Int,
       Class3 = Int,
       Weight = Double)

Create(Rules_6,
       Head   = Int,
       Body1  = Int,
       Body2  = Int,
       Class1 = Int,
       Class2 = Int,
       Class3 = Int,
       Weight = Double)
